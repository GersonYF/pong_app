import { StyleSheet } from "react-native";
import COLORS from "../../styles/colors";

const STL_GAMES = StyleSheet.create({
  content: {
    backgroundColor: COLORS.white,
    flex: 1,
    borderRadius: 20
  }
});

export default STL_GAMES;
